{
  "name": "rate-limiter",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "watch": "bun --watch run index.ts",
    "build": "bun build ./index.ts --outdir ./out",
    "hot": "bun --hot run index.ts"
  },
  "dependencies": {
    "hono": "^3.7.5"
  }
}